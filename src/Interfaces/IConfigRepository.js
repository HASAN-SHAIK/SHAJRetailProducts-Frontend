/**
 * @typedef {object} IConfigRepository
 * @property {(key: string, value: unknown) => Promise<void>} saveConfigValue
 * @property {(key: string) => Promise<unknown>} getConfigValue
 */

export {};
