name: V1 Frontend completion acceptance

on:
  pull_request:
    paths:
      - 'src/utils/appSyncOrchestrator.js'
      - 'src/utils/appSyncOrchestrator.v1.test.js'
      - '.github/workflows/v1-frontend-completion.yml'
      - 'package.json'
      - 'package-lock.json'
  workflow_dispatch:

jobs:
  certify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Focused V1 Frontend sync authority acceptance
        run: >-
          CI=true npm test -- --watchAll=false
          src/utils/appSyncOrchestrator.v1.test.js
      - name: Production build
        run: CI=false npm run build
