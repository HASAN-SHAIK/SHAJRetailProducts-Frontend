name: V1 Cycle C Login Credential Semantics Runtime

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      EXPECTED_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
      REACT_APP_CENTRAL_API_URL: http://127.0.0.1:43194/api
      REACT_APP_POS_LOCAL_API_ENABLED: 'false'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
      - name: Assert exact tested head
        run: test "$(git rev-parse HEAD)" = "${EXPECTED_HEAD}"
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install
        run: npm ci --no-audit --no-fund
      - name: Focused credential-semantics acceptance
        id: focused
        run: |
          set +e
          set -o pipefail
          CI=true npm test -- --watchAll=false --runTestsByPath src/components/Login/loginInteraction.v1.test.js -t 'associates labels and browser credential semantics' 2>&1 | tee /tmp/cycle-c-login-credential-focused.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          exit 0
      - name: CI production build gate
        id: build
        run: |
          set +e
          set -o pipefail
          CI=true npm run build 2>&1 | tee /tmp/cycle-c-login-credential-build.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Build runnable production bundle
        id: runtime_build
        run: |
          set +e
          set -o pipefail
          CI=false npm run build 2>&1 | tee /tmp/cycle-c-login-credential-runtime-build.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Start runnable production frontend
        id: startup
        if: steps.runtime_build.outputs.exit_code == '0'
        run: |
          set +e
          npx --yes serve@14 -s build -l 4187 > /tmp/cycle-c-login-credential-server.log 2>&1 &
          echo $! > /tmp/cycle-c-login-credential-server.pid
          RC=1
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:4187/ >/tmp/cycle-c-login-credential.html; then RC=0; break; fi
            sleep 1
          done
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Real browser login credential semantics path
        id: runtime
        if: steps.startup.outputs.exit_code == '0'
        env:
          CYPRESS_BASE_URL: http://127.0.0.1:4187
        run: |
          set +e
          set -o pipefail
          npx cypress run --browser electron --spec cypress/e2e/runtime/login-credential-semantics-runtime.cy.js 2>&1 | tee /tmp/cycle-c-login-credential-runtime.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Post-runtime process health
        id: health
        if: always()
        run: |
          set +e
          RC=0
          test -f /tmp/cycle-c-login-credential-server.pid || RC=1
          if [ "$RC" = 0 ]; then
            PID="$(cat /tmp/cycle-c-login-credential-server.pid)"
            kill -0 "$PID" || RC=1
          fi
          STATUS="000"
          if [ "$RC" = 0 ]; then
            STATUS="$(curl -sS -o /tmp/cycle-c-login-credential-post.html -w '%{http_code}' http://127.0.0.1:4187/)" || RC=1
            test "$STATUS" = "200" || RC=1
          fi
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_CREDENTIAL_PROCESS_ALIVE=$([ "$RC" = 0 ] && echo true || echo false)"
          echo "CYCLE_C_LOGIN_CREDENTIAL_POST_RUNTIME_HTTP=${STATUS}"
          exit 0
      - name: Complete Frontend suite
        id: suite
        if: always()
        run: |
          set +e
          set -o pipefail
          CI=true npm test -- --watchAll=false 2>&1 | tee /tmp/cycle-c-login-credential-suite.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          exit 0
      - name: Evidence summary
        if: always()
        run: |
          echo "CYCLE_C_EXACT_HEAD=$(git rev-parse HEAD)"
          echo "CYCLE_C_LOGIN_CREDENTIAL_FOCUSED_EXIT_CODE=${{ steps.focused.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_CREDENTIAL_BUILD_EXIT_CODE=${{ steps.build.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_CREDENTIAL_RUNTIME_BUILD_EXIT_CODE=${{ steps.runtime_build.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_CREDENTIAL_STARTUP_EXIT_CODE=${{ steps.startup.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_CREDENTIAL_RUNTIME_EXIT_CODE=${{ steps.runtime.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_CREDENTIAL_HEALTH_EXIT_CODE=${{ steps.health.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_CREDENTIAL_SUITE_EXIT_CODE=${{ steps.suite.outputs.exit_code }}"
          cat /tmp/cycle-c-login-credential-focused.log || true
          cat /tmp/cycle-c-login-credential-runtime.log || true
      - name: Upload retained evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cycle-c-login-credential-semantics-evidence
          path: |
            /tmp/cycle-c-login-credential-focused.log
            /tmp/cycle-c-login-credential-runtime.log
            /tmp/cycle-c-login-credential-build.log
            /tmp/cycle-c-login-credential-runtime-build.log
            /tmp/cycle-c-login-credential-suite.log
            /tmp/cycle-c-login-credential-server.log
            /tmp/cycle-c-login-credential.html
            /tmp/cycle-c-login-credential-post.html
          if-no-files-found: warn
      - name: Assert focused gate
        if: always()
        run: test "${{ steps.focused.outputs.exit_code }}" = "0"
      - name: Assert runtime build gate
        if: always()
        run: test "${{ steps.runtime_build.outputs.exit_code }}" = "0"
      - name: Assert runtime gate
        if: always()
        run: test "${{ steps.runtime.outputs.exit_code }}" = "0"
      - name: Assert health gate
        if: always()
        run: test "${{ steps.health.outputs.exit_code }}" = "0"
      - name: Assert suite gate
        if: always()
        run: test "${{ steps.suite.outputs.exit_code }}" = "0"
      - name: Assert CI build gate
        if: always()
        run: test "${{ steps.build.outputs.exit_code }}" = "0"
