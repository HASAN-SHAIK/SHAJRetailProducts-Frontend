name: V1 Cycle C Login Loading Live Status Runtime

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  certify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      EXPECTED_HEAD: ${{ github.event.pull_request.head.sha || github.sha }}
      REACT_APP_CENTRAL_API_URL: http://127.0.0.1:43193/api
      REACT_APP_POS_LOCAL_API_ENABLED: 'false'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 1
      - name: Assert exact tested head
        run: test "$(git rev-parse HEAD)" = "${EXPECTED_HEAD}"
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install
        run: npm ci --no-audit --no-fund
      - name: Existing focused login accessibility acceptance
        id: focused
        run: |
          set +e
          set -o pipefail
          CI=true npm test -- --watchAll=false --runTestsByPath src/components/Login/loginInteraction.v1.test.js 2>&1 | tee /tmp/cycle-c-login-loading-focused.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_LOADING_FOCUSED_EXIT_CODE=${RC}"
          exit 0
      - name: CI production build gate
        id: build
        run: |
          set +e
          set -o pipefail
          CI=true npm run build 2>&1 | tee /tmp/cycle-c-login-loading-build.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_LOADING_BUILD_EXIT_CODE=${RC}"
          exit 0
      - name: Build runnable production bundle
        id: runtime_build
        run: |
          set +e
          set -o pipefail
          CI=false npm run build 2>&1 | tee /tmp/cycle-c-login-loading-runtime-build.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_LOADING_RUNTIME_BUILD_EXIT_CODE=${RC}"
          exit 0
      - name: Start runnable production frontend
        id: startup
        if: steps.runtime_build.outputs.exit_code == '0'
        run: |
          set +e
          npx --yes serve@14 -s build -l 4186 > /tmp/cycle-c-login-loading-server.log 2>&1 &
          echo $! > /tmp/cycle-c-login-loading-server.pid
          RC=1
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:4186/ >/tmp/cycle-c-login-loading.html; then RC=0; break; fi
            sleep 1
          done
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_LOADING_STARTUP_EXIT_CODE=${RC}"
          exit 0
      - name: Real browser pending-login live-status path
        id: runtime
        if: steps.startup.outputs.exit_code == '0'
        env:
          CYPRESS_BASE_URL: http://127.0.0.1:4186
        run: |
          set +e
          set -o pipefail
          npx cypress run --browser electron --spec cypress/e2e/runtime/login-loading-live-status-runtime.cy.js 2>&1 | tee /tmp/cycle-c-login-loading-runtime.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_LOADING_RUNTIME_EXIT_CODE=${RC}"
          exit 0
      - name: Post-runtime process health
        id: health
        if: always()
        run: |
          set +e
          RC=0
          test -f /tmp/cycle-c-login-loading-server.pid || RC=1
          if [ "$RC" = 0 ]; then
            PID="$(cat /tmp/cycle-c-login-loading-server.pid)"
            kill -0 "$PID" || RC=1
          fi
          STATUS="000"
          if [ "$RC" = 0 ]; then
            STATUS="$(curl -sS -o /tmp/cycle-c-login-loading-post.html -w '%{http_code}' http://127.0.0.1:4186/)" || RC=1
            test "$STATUS" = "200" || RC=1
          fi
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_LOADING_PROCESS_ALIVE=$([ "$RC" = 0 ] && echo true || echo false)"
          echo "CYCLE_C_LOGIN_LOADING_POST_RUNTIME_HTTP=${STATUS}"
          exit 0
      - name: Complete Frontend suite
        id: suite
        if: always()
        run: |
          set +e
          set -o pipefail
          CI=true npm test -- --watchAll=false 2>&1 | tee /tmp/cycle-c-login-loading-suite.log
          RC=${PIPESTATUS[0]}
          echo "exit_code=${RC}" >> "$GITHUB_OUTPUT"
          echo "CYCLE_C_LOGIN_LOADING_SUITE_EXIT_CODE=${RC}"
          exit 0
      - name: Evidence summary
        if: always()
        run: |
          echo "CYCLE_C_EXACT_HEAD=$(git rev-parse HEAD)"
          echo "CYCLE_C_LOGIN_LOADING_FOCUSED_EXIT_CODE=${{ steps.focused.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_LOADING_BUILD_EXIT_CODE=${{ steps.build.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_LOADING_RUNTIME_BUILD_EXIT_CODE=${{ steps.runtime_build.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_LOADING_STARTUP_EXIT_CODE=${{ steps.startup.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_LOADING_RUNTIME_EXIT_CODE=${{ steps.runtime.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_LOADING_HEALTH_EXIT_CODE=${{ steps.health.outputs.exit_code }}"
          echo "CYCLE_C_LOGIN_LOADING_SUITE_EXIT_CODE=${{ steps.suite.outputs.exit_code }}"
          echo '--- focused ---'
          cat /tmp/cycle-c-login-loading-focused.log || true
          echo '--- runtime ---'
          cat /tmp/cycle-c-login-loading-runtime.log || true
          echo '--- CI build tail ---'
          tail -n 100 /tmp/cycle-c-login-loading-build.log || true
          echo '--- runtime build tail ---'
          tail -n 60 /tmp/cycle-c-login-loading-runtime-build.log || true
          echo '--- suite tail ---'
          tail -n 120 /tmp/cycle-c-login-loading-suite.log || true
          echo '--- server log ---'
          cat /tmp/cycle-c-login-loading-server.log || true
      - name: Upload retained evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cycle-c-login-loading-live-status-evidence
          path: |
            /tmp/cycle-c-login-loading-focused.log
            /tmp/cycle-c-login-loading-runtime.log
            /tmp/cycle-c-login-loading-build.log
            /tmp/cycle-c-login-loading-runtime-build.log
            /tmp/cycle-c-login-loading-suite.log
            /tmp/cycle-c-login-loading-server.log
            /tmp/cycle-c-login-loading.html
            /tmp/cycle-c-login-loading-post.html
          if-no-files-found: warn
      - name: Assert focused gate
        if: always()
        run: test "${{ steps.focused.outputs.exit_code }}" = "0"
      - name: Assert runtime build gate
        if: always()
        run: test "${{ steps.runtime_build.outputs.exit_code }}" = "0"
      - name: Assert runtime gate
        if: always()
        run: test "${{ steps.runtime.outputs.exit_code }}" = "0"
      - name: Assert health gate
        if: always()
        run: test "${{ steps.health.outputs.exit_code }}" = "0"
      - name: Assert suite gate
        if: always()
        run: test "${{ steps.suite.outputs.exit_code }}" = "0"
      - name: Assert CI build gate
        if: always()
        run: test "${{ steps.build.outputs.exit_code }}" = "0"
