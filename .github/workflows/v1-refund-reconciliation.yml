name: V1 refund reconciliation

on:
  pull_request:
    paths:
      - 'src/pages/returnsCorrections/**'
      - 'src/services/local/**'
      - 'src/services/posSyncRecoveryService.js'
      - 'src/Repositories/local/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/v1-refund-reconciliation.yml'

jobs:
  certify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Focused refund reconciliation tests
        run: |
          CI=true npm test -- --watchAll=false \
            src/pages/returnsCorrections/refundReconciliationPolicy.test.js \
            src/pages/returnsCorrections/RefundReconciliationPanel.test.js \
            src/pages/returnsCorrections/RefundRecoveryLifecycleAcceptance.test.js
      - name: Production build
        # The repository currently has unrelated legacy ESLint warnings. Keep this
        # focused V1 gate responsible for compile integrity without converting those
        # pre-existing warnings into failures for an otherwise isolated refund patch.
        run: CI=false npm run build
